{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@parcel/static-build" }
  ],
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
